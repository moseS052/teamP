<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.team.pj.iteamP">
<select id="listMember" resultType="com.team.pj.memberDTO">
	select * from member 
</select>
<insert id="insert">
	insert into member values(member_sq.nextval,#{param1},#{param2},#{param3},#{param4},#{param5},#{param6},#{param7},#{param8})
</insert>
<select id="count" resultType="int">
	select count(*) cnt from member where id=#{param1} and pw=#{param2}
</select>
</mapper>